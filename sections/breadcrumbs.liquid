{% assign ai_gen_id = section.id | replace: '_', '' | downcase %}

{% style %}
  .ai-breadcrumbs-{{ ai_gen_id }} {
    padding: {{ section.settings.padding_top }}px 0 {{ section.settings.padding_bottom }}px;
    background-color: {{ section.settings.background_color }};
  }

  .ai-breadcrumbs-container-{{ ai_gen_id }} {
    max-width: 1400px;
  }

  .ai-breadcrumbs-nav-{{ ai_gen_id }} {
    font-size: {{ section.settings.font_size }}px;
    color: {{ section.settings.text_color }};
  }

  .ai-breadcrumbs-list-{{ ai_gen_id }} {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    list-style: none;
    margin: 0;
    padding: 0;
    gap: 8px;
  }

  .ai-breadcrumbs-item-{{ ai_gen_id }} {
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .ai-breadcrumbs-link-{{ ai_gen_id }} {
    color: {{ section.settings.link_color }};
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .ai-breadcrumbs-link-{{ ai_gen_id }}:hover {
    color: {{ section.settings.link_hover_color }};
    text-decoration: underline;
  }

  .ai-breadcrumbs-current-{{ ai_gen_id }} {
    color: {{ section.settings.current_color }};
    font-weight: 500;
  }

  .ai-breadcrumbs-separator-{{ ai_gen_id }} {
    color: {{ section.settings.separator_color }};
    font-size: {{ section.settings.font_size | times: 0.8 }}px;user-select: none;
  }

  @media screen and (max-width: 749px) {
    .ai-breadcrumbs-{{ ai_gen_id }} {
      padding: {{ section.settings.padding_top | times: 0.7 }}px 0 {{ section.settings.padding_bottom | times: 0.7 }}px;
    }

    .ai-breadcrumbs-nav-{{ ai_gen_id }} {
      font-size: {{ section.settings.font_size | times: 0.9 }}px;
    }

    .ai-breadcrumbs-container-{{ ai_gen_id }} {
      padding: 0 15px;
    }
  }
{% endstyle %}

<div class="ai-breadcrumbs-{{ ai_gen_id }}  breadcrumbs_section" {{ section.shopify_attributes }}>
  <div class="ai-breadcrumbs-container-{{ ai_gen_id }}">
    <nav class="ai-breadcrumbs-nav-{{ ai_gen_id }}" aria-label="Breadcrumb">
      <ol class="ai-breadcrumbs-list-{{ ai_gen_id }}"><li class="ai-breadcrumbs-item-{{ ai_gen_id }}">
          <a href="{{ routes.root_url }}" class="ai-breadcrumbs-link-{{ ai_gen_id }}">
            {{ section.settings.home_text }}
          </a>
        </li>

        {% case template.name %}
          {% when 'collection' %}
            <li class="ai-breadcrumbs-item-{{ ai_gen_id }}">
              <span class="ai-breadcrumbs-separator-{{ ai_gen_id }}">{{ section.settings.separator }}</span>
              <span class="ai-breadcrumbs-current-{{ ai_gen_id }}">{{ collection.title }}</span>
            </li>

          {% when 'product' %}
            {% if collection %}
              <li class="ai-breadcrumbs-item-{{ ai_gen_id }}">
                <span class="ai-breadcrumbs-separator-{{ ai_gen_id }}">{{ section.settings.separator }}</span><a href="{{ collection.url }}" class="ai-breadcrumbs-link-{{ ai_gen_id }}">
                  {{ collection.title }}
                </a>
              </li>
            {% endif %}<li class="ai-breadcrumbs-item-{{ ai_gen_id }}">
              <span class="ai-breadcrumbs-separator-{{ ai_gen_id }}">{{ section.settings.separator }}</span>
              <span class="ai-breadcrumbs-current-{{ ai_gen_id }}">{{ product.title }}</span>
            </li>

          {% when 'page' %}
            <li class="ai-breadcrumbs-item-{{ ai_gen_id }}">
              <span class="ai-breadcrumbs-separator-{{ ai_gen_id }}">{{ section.settings.separator }}</span>
              <span class="ai-breadcrumbs-current-{{ ai_gen_id }}">{{ page.title }}</span>
            </li>

          {% when 'blog' %}
            <li class="ai-breadcrumbs-item-{{ ai_gen_id }}">
              <span class="ai-breadcrumbs-separator-{{ ai_gen_id }}">{{ section.settings.separator }}</span>
              <span class="ai-breadcrumbs-current-{{ ai_gen_id }}">{{ blog.title }}</span>
            </li>

          {% when 'article' %}
            <li class="ai-breadcrumbs-item-{{ ai_gen_id }}">
              <span class="ai-breadcrumbs-separator-{{ ai_gen_id }}">{{ section.settings.separator }}</span><a href="{{ blog.url }}" class="ai-breadcrumbs-link-{{ ai_gen_id }}">
                {{ blog.title }}
              </a>
            </li><li class="ai-breadcrumbs-item-{{ ai_gen_id }}">
              <span class="ai-breadcrumbs-separator-{{ ai_gen_id }}">{{ section.settings.separator }}</span>
              <span class="ai-breadcrumbs-current-{{ ai_gen_id }}">{{ article.title }}</span>
            </li>

          {% when 'cart' %}
            <li class="ai-breadcrumbs-item-{{ ai_gen_id }}">
              <span class="ai-breadcrumbs-separator-{{ ai_gen_id }}">{{ section.settings.separator }}</span>
              <span class="ai-breadcrumbs-current-{{ ai_gen_id }}">{{ section.settings.cart_text }}</span>
            </li>

          {% when 'search' %}
            <li class="ai-breadcrumbs-item-{{ ai_gen_id }}">
              <span class="ai-breadcrumbs-separator-{{ ai_gen_id }}">{{ section.settings.separator }}</span>
              <span class="ai-breadcrumbs-current-{{ ai_gen_id }}">
                {% if search.performed %}
                  {{ section.settings.search_results_text }}: "{{ search.terms }}"
                {% else %}
                  {{ section.settings.search_text }}
                {% endif %}
              </span>
            </li>

          {% when '404' %}
            <li class="ai-breadcrumbs-item-{{ ai_gen_id }}">
              <span class="ai-breadcrumbs-separator-{{ ai_gen_id }}">{{ section.settings.separator }}</span>
              <span class="ai-breadcrumbs-current-{{ ai_gen_id }}">{{ section.settings.not_found_text }}</span>
            </li>

          {% when 'customers/account' %}
            <li class="ai-breadcrumbs-item-{{ ai_gen_id }}">
              <span class="ai-breadcrumbs-separator-{{ ai_gen_id }}">{{ section.settings.separator }}</span>
              <span class="ai-breadcrumbs-current-{{ ai_gen_id }}">{{ section.settings.account_text }}</span>
            </li>

          {% when 'customers/login' %}
            <li class="ai-breadcrumbs-item-{{ ai_gen_id }}">
              <span class="ai-breadcrumbs-separator-{{ ai_gen_id }}">{{ section.settings.separator }}</span>
              <span class="ai-breadcrumbs-current-{{ ai_gen_id }}">{{ section.settings.login_text }}</span>
            </li>

          {% when 'customers/register' %}
            <li class="ai-breadcrumbs-item-{{ ai_gen_id }}">
              <span class="ai-breadcrumbs-separator-{{ ai_gen_id }}">{{ section.settings.separator }}</span>
              <span class="ai-breadcrumbs-current-{{ ai_gen_id }}">{{ section.settings.register_text }}</span>
            </li>

          {% when 'customers/order' %}
            <li class="ai-breadcrumbs-item-{{ ai_gen_id }}">
              <span class="ai-breadcrumbs-separator-{{ ai_gen_id }}">{{ section.settings.separator }}</span>
              <a href="{{ routes.account_url }}" class="ai-breadcrumbs-link-{{ ai_gen_id }}">
                {{ section.settings.account_text }}
              </a>
            </li>
            <li class="ai-breadcrumbs-item-{{ ai_gen_id }}">
              <span class="ai-breadcrumbs-separator-{{ ai_gen_id }}">{{ section.settings.separator }}</span>
              <span class="ai-breadcrumbs-current-{{ ai_gen_id }}">{{ section.settings.order_text }}</span>
            </li>

          {% else %}
            {% unless template.name == 'index' %}
              <li class="ai-breadcrumbs-item-{{ ai_gen_id }}">
                <span class="ai-breadcrumbs-separator-{{ ai_gen_id }}">{{ section.settings.separator }}</span>
                <span class="ai-breadcrumbs-current-{{ ai_gen_id }}">{{ page_title }}</span>
              </li>
            {% endunless %}
        {% endcase %}
      </ol>
    </nav>
  </div>
</div>

{% schema %}
{
  "name": "Breadcrumbs",
"tag": "section",
"class": "section x-section breadcrumbs",
  "settings": [

    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "home_text",
      "label": "Home text",
      "default": "Home"
    },
    {
      "type": "text",
      "id": "separator",
      "label": "Separator",
      "default": "/"
    },
    {
      "type": "text",
      "id": "cart_text",
      "label": "Cart text",
      "default": "Cart"
    },
    {
      "type": "text",
      "id": "search_text",
      "label": "Search text",
      "default": "Search"
    },
    {
      "type": "text",
      "id": "search_results_text",
      "label": "Search results text",
      "default": "Search results"
    },
    {
      "type": "text",
      "id": "not_found_text",
      "label": "404 page text",
      "default": "Page not found"
    },
    {
      "type": "text",
      "id": "account_text",
      "label": "Account text",
      "default": "Account"
    },
    {
      "type": "text",
      "id": "login_text",
      "label": "Login text",
      "default": "Login"
    },
    {
      "type": "text",
      "id": "register_text",
      "label": "Register text",
      "default": "Create account"
    },
    {
      "type": "text",
      "id": "order_text",
      "label": "Order text",
      "default": "Order"
    },
    {
      "type": "header",
      "content": "Style"
    },
    {
      "type": "range",
      "id": "font_size",
      "min": 12,
      "max": 20,
      "step": 1,
      "unit": "px",
      "label": "Font size",
      "default": 14
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color",
      "default": "#666666"
    },
    {
      "type": "color",
      "id": "link_color",
      "label": "Link color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "link_hover_color",
      "label": "Link hover color",
      "default": "#000f9f"
    },
    {
      "type": "color",
      "id": "current_color",
      "label": "Current page color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "separator_color",
      "label": "Separator color",
      "default": "#cccccc"
    },
    {
      "type": "header",
      "content": "Spacing"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Top padding",
      "default": 20
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Bottom padding",
      "default": 20
    }
  ],
  "presets": [
    {
      "name": "Breadcrumbs"
    }
  ]
}
{% endschema %}